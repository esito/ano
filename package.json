{
  "scripts": {
    "watch": "cd vscode && npm run watch",
    "build": "cd vscode && npm run build",
    "compile": "cd vscode && npm run compile",
    "compileAntlr": "cd antlr && npm run compile"
  }
}
